import { Paper } from "@mui/material"

const DiscountPercent =props =>{
    return <Paper sx={{width:'4em', padding:' 0.2em', backgroundColor:'primary.main', color:'primary.contrastText'}} elevation={0}  >
        44
    </Paper>
}

export default DiscountPercent